<h1>TaskList list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Listtitle</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('tasklist_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.listTitle }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('tasklist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tasklist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('tasklist_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
